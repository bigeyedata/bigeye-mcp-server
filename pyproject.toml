[project]
name = "bigeye-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    # 1.13+ exposes mcp.server.streamable_http_manager, which the BYOK
    # HTTP transport in transport_http.py mounts under /mcp.
    "mcp[cli]>=1.13.0",
    "cryptography>=43.0.0",
    "starlette>=0.40.0",
    "uvicorn>=0.30.0",
]
